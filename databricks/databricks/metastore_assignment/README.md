# databricks_metastore_assignment

This Terraform module was generated by [IAC Foundry](https://iacfoundry.com) on 2026.04.27.

- **Developer**: `databricks`
- **Provider**: `databricks`
- **Version**: `v1.96.0`



## Example Module Call

```hcl
module "metastore_assignment" {
  source = "./modules/databricks/databricks/metastore_assignment"

  databricks_metastore_assignment_data = var.databricks_metastore_assignment_data
}
```


## Generated `variables.tf`

```hcl
variable "databricks_metastore_assignment_data" {
  type = map(object({
    # Required
    enabled = bool
    metastore_id = optional(string) # Set to Optional for allowed lookup by name value
    metastore_name = optional(string) # Optional alias for metastore_id
    workspace_id = optional(number) # Set to Optional for allowed lookup by name value
    workspace_name = optional(string) # Optional alias for workspace_id
    
    # Optional
    default_catalog_name = optional(string)
  }))
  default = {}
}

```
