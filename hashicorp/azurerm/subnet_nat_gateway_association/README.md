# azurerm_subnet_nat_gateway_association

This Terraform module was generated by [IAC Foundry](https://iacfoundry.com) on 2026.05.03.

- **Developer**: `hashicorp`
- **Provider**: `azurerm`
- **Version**: `v4.71.0`


## Example `main.tf`

```hcl
module "subnet_nat_gateway_association" {
  source = "./modules/hashicorp/azurerm/subnet_nat_gateway_association"

  azurerm_subnet_nat_gateway_association_data = module.azurerm_defaults.merge["azurerm_subnet_nat_gateway_association"]
}
```



## Generated `variables.tf`

```hcl
variable "azurerm_subnet_nat_gateway_association_data" {
  type = map(object({
    # Required
    enabled = bool
    nat_gateway_id = optional(string) # Set to Optional for allowed lookup by name value
    nat_gateway_name = optional(string) # Optional alias for nat_gateway_id
    subnet_id = optional(string) # Set to Optional for allowed lookup by name value
    subnet_name = optional(string) # Optional alias for subnet_id
  }))
  default = {}
}

```
