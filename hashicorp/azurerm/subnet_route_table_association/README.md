# azurerm_subnet_route_table_association

This Terraform module was generated by [IAC Foundry](https://iacfoundry.com) on 2026.05.03.

- **Developer**: `hashicorp`
- **Provider**: `azurerm`
- **Version**: `v4.71.0`


## Example `main.tf`

```hcl
module "subnet_route_table_association" {
  source = "./modules/hashicorp/azurerm/subnet_route_table_association"

  azurerm_subnet_route_table_association_data = module.azurerm_defaults.merge["azurerm_subnet_route_table_association"]
}
```



## Generated `variables.tf`

```hcl
variable "azurerm_subnet_route_table_association_data" {
  type = map(object({
    # Required
    enabled = bool
    route_table_id = optional(string) # Set to Optional for allowed lookup by name value
    route_table_name = optional(string) # Optional alias for route_table_id
    subnet_id = optional(string) # Set to Optional for allowed lookup by name value
    subnet_name = optional(string) # Optional alias for subnet_id
  }))
  default = {}
}

```
